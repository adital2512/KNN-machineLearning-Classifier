#include <cmath>
#include <vector>
#include <string>
#include <iostream>
using namespace std;
typedef std::vector<double> vec;
double EuclideanDistance(vec, vec);
double ManhattanDistance(vec, vec);
double ChebyshevDistance(vec, vec);
double CanberraDistance(vec, vec);
double MinkowskiDistance(vec, vec);